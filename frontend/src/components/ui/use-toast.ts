/**
 * Re-export the shared toast store used by `<Toaster />`.
 * Import from either `@/hooks/use-toast` or `@/components/ui/use-toast`.
 */
export { useToast, toast, reducer } from "@/hooks/use-toast"
